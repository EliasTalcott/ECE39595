public class Structure extends Displayable {
    public Structure(int _visible, int _posX, int _posY) { super(_visible, _posX, _posY); }

    public Structure(int _posX, int _posY) { super(_posX, _posY); }

    @Override
    public String toString() { return super.toString(); }
}
